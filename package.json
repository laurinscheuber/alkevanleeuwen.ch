{
  "name": "psychiotherapie-alkevanleeuwen",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:studio": "cd studio && npm run dev",
    "build": "npm run build:astro && npm run build:studio",
    "build:astro": "astro build",
    "build:studio": "cd studio && npm install && npm run build && mkdir -p ../dist/studio && cp -r dist/* ../dist/studio",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@astrojs/tailwind": "^6.0.2",
    "@sanity/client": "^7.14.1",
    "@sanity/image-url": "^2.0.3",
    "astro": "^5.17.1",
    "astro-portabletext": "^0.13.0",
    "tailwindcss": "^3.4.19"
  }
}
