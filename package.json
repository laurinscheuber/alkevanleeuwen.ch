{
  "name": "psychiotherapie-alkevanleeuwen",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": "23.x"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:studio": "cd studio && npm run dev",
    "build": "npm run build:astro && npm run build:studio",
    "build:astro": "astro build",
    "build:studio": "cd studio && npm install && npm run build && mkdir -p ../dist/client/studio && cp -r dist/* ../dist/client/studio",
    "preview": "astro preview",
    "start": "HOST=0.0.0.0 PORT=${PORT:-4321} node ./dist/server/entry.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/node": "^10.0.4",
    "@astrojs/sitemap": "^3.7.2",
    "@astrojs/tailwind": "^6.0.2",
    "@fontsource/outfit": "^5.2.8",
    "@fontsource/playfair-display": "^5.2.8",
    "@sanity/client": "^7.14.1",
    "@sanity/image-url": "^2.0.3",
    "astro": "^6.1.1",
    "astro-portabletext": "^0.13.0",
    "nodemailer": "^8.0.4",
    "tailwindcss": "^3.4.19"
  }
}
