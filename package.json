{
  "name": "alkevanleeuwen.ch",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cd studio && npm install && npm run build && cd .. && mkdir -p dist/studio && cp -r studio/dist/* dist/studio/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@sanity/client": "^7.13.1",
    "@sanity/image-url": "^2.0.2"
  }
}
